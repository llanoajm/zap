# Loop Handoff

This file is overwritten by `loop.sh` at the start of each iteration with the
current item and protocol, and is appended to by the verify phase. It is the
loop's per-iteration scratchpad — not a human-maintained doc.

(empty — will be populated on first iteration)
