# Microfluidic droplet-control project dependencies (separate from the zap
# power-systems poetry env). CPU-only is sufficient; CUDA/CoreML used if present.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.1
opencv-python-headless>=4.8
matplotlib>=3.7
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1
coremltools>=7.0        # CoreML export (conversion runs on Linux; predict needs macOS)
openpyxl>=3.1           # reading the DAFD .xlsx datasets
pillow>=10.0            # synthetic renderer fallback
pytest>=7.4
# pyserial>=3.5         # only for real Harvard pumps (deferred import)
# zwoasi                # only for the real ZWO ASI camera (deferred import)
# scipy                 # optional, DoF blur in the synthetic renderer
